/* eslint-disable no-undef */

// Глобальная переменная root
const root = document.querySelector('.root');

// header-ligth elements
const headerLight = root.querySelector('.header_light');
const menuButtonLight = headerLight.querySelector('.header__toggle-menu_light');
const headerMenuLight = headerLight.querySelector('.header-menu');

export {
  headerLight,
  menuButtonLight,
  headerMenuLight,
};
